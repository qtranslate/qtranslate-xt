'use strict';

export const EditorMode = Object.freeze({
    LSB: 0,
    RAW: 1,
    SINGLE: 2,
});

export const UrlMode = Object.freeze({
    QUERY: 1,
    PATH: 2,
    DOMAIN: 3,
    DOMAINS: 4,
});

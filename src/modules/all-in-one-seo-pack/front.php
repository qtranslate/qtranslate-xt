<?php
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
/*
 * function internationalize( $in ) from all-in-one-seo-pack/aioseop_class.php
 * does the job with qtrans_useCurrentLanguageIfNotFoundUseDefaultLanguage
 */

/*
function qtranxf_aioseop_add_filters_front() {
	$use_filters = array(
		'aiosp_title' => 20,
		'aiosp_meta' => 20,
		'aiosp_metadesc' => 20,
	);
	foreach ( $use_filters as $name => $priority ) {
		add_filter( $name, 'qtranxf_useCurrentLanguageIfNotFoundUseDefaultLanguage', $priority );
	}
}
qtranxf_aioseop_add_filters_front();
*/
